[project]
name = "uv-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
prod = [
    "sentry-sdk>=2.26.0",
]